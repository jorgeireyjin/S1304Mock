package com.mycompany;

// Clase que simula el servicio de procesamiento de pagos
public class PaymentService {

    public boolean processPayment(double amount) {
        // Simula el procesamiento de un pago, que normalmente interactuaría con un sistema de pagos externo
        System.out.println("Procesando pago de " + amount + " unidades.");
        return true; // Simulamos que el pago fue exitoso
    }
}
